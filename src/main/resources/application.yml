server:
  port: 8081
  
spring.jpa:
  database: MYSQL
  hibernate.ddl-auto: update
  show-sql: true

spring.datasource:
  platform: mysql
  driverClassName: com.mysql.jdbc.Driver
  url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/db1?serverTimezone=UTC&useLegacyDatetimeCode=false
  username: root
  password: rammstein